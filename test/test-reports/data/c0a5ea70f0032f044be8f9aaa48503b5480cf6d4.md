# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 02-navigation.spec.ts >> 02 导航和菜单测试 >> 02-01 侧边栏应可见且包含所有一级模块
- Location: test/e2e/02-navigation.spec.ts:28:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.ant-layout-sider, .ant-menu, [class*="sider"], [class*="sidebar"], aside').first()
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('.ant-layout-sider, .ant-menu, [class*="sider"], [class*="sidebar"], aside').first()

```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | import {
  3   |   bypassLogin,
  4   |   navigateTo,
  5   |   takeScreenshot,
  6   |   collectErrors,
  7   | } from '../utils/test-helpers';
  8   | import { SIDEBAR_MODULES } from '../fixtures/test-data';
  9   | 
  10  | const filterApiErrors = (errors: string[]) =>
  11  |   errors.filter(
  12  |     (e) =>
  13  |       !e.includes('Failed to fetch') &&
  14  |       !e.includes('NetworkError') &&
  15  |       !e.includes('net::ERR') &&
  16  |       !e.includes('404') &&
  17  |       !e.includes('502') &&
  18  |       !e.includes('503') &&
  19  |       !e.includes('ERR_CONNECTION_REFUSED')
  20  |   );
  21  | 
  22  | test.describe('02 导航和菜单测试', () => {
  23  |   test.beforeEach(async ({ page }) => {
  24  |     await bypassLogin(page);
  25  |     await navigateTo(page, '/');
  26  |   });
  27  | 
  28  |   test('02-01 侧边栏应可见且包含所有一级模块', async ({ page }) => {
  29  |     const errors = collectErrors(page);
  30  | 
  31  |     // 等待侧边栏渲染
  32  |     const sidebar = page
  33  |       .locator(
  34  |         '.ant-layout-sider, .ant-menu, [class*="sider"], [class*="sidebar"], aside'
  35  |       )
  36  |       .first();
> 37  |     await expect(sidebar).toBeVisible({ timeout: 10000 });
      |                           ^ Error: expect(locator).toBeVisible() failed
  38  | 
  39  |     // 验证每个一级模块在侧边栏中
  40  |     for (const mod of SIDEBAR_MODULES) {
  41  |       const menuItem = page
  42  |         .locator('.ant-menu-item, .ant-menu-submenu-title')
  43  |         .filter({ hasText: mod.name });
  44  |       await expect(menuItem.first()).toBeVisible({ timeout: 5000 });
  45  |     }
  46  | 
  47  |     await takeScreenshot(page, '02-sidebar-all-modules');
  48  |     expect(filterApiErrors(errors)).toEqual([]);
  49  |   });
  50  | 
  51  |   test('02-02 点击菜单项应能导航到对应路径', async ({ page }) => {
  52  |     const errors = collectErrors(page);
  53  | 
  54  |     // 展开「埋点管理」子菜单
  55  |     const submenuTitle = page
  56  |       .locator('.ant-menu-submenu-title')
  57  |       .filter({ hasText: '埋点管理' });
  58  |     await submenuTitle.click();
  59  |     await page.waitForTimeout(500);
  60  | 
  61  |     // 点击「事件管理」
  62  |     const childItem = page
  63  |       .locator('.ant-menu-item')
  64  |       .filter({ hasText: '事件管理' });
  65  |     await childItem.first().click();
  66  |     await page.waitForTimeout(1000);
  67  | 
  68  |     await expect(page).toHaveURL(/\/point-manage\/event/);
  69  |     await takeScreenshot(page, '02-nav-click-event-manage');
  70  | 
  71  |     // 展开「数据分析」子菜单
  72  |     const dataSubmenu = page
  73  |       .locator('.ant-menu-submenu-title')
  74  |       .filter({ hasText: '数据分析' });
  75  |     await dataSubmenu.click();
  76  |     await page.waitForTimeout(500);
  77  | 
  78  |     // 点击「事件分析」
  79  |     const eventAnalysis = page
  80  |       .locator('.ant-menu-item')
  81  |       .filter({ hasText: '事件分析' });
  82  |     await eventAnalysis.first().click();
  83  |     await page.waitForTimeout(1000);
  84  | 
  85  |     await expect(page).toHaveURL(/\/data-analysis\/event/);
  86  |     await takeScreenshot(page, '02-nav-click-data-analysis');
  87  | 
  88  |     expect(filterApiErrors(errors)).toEqual([]);
  89  |   });
  90  | 
  91  |   test('02-03 所有主要路径都能加载无崩溃', async ({ page }) => {
  92  |     const errors = collectErrors(page);
  93  | 
  94  |     const paths = [
  95  |       '/',
  96  |       '/point-manage/event',
  97  |       '/data-analysis/event',
  98  |       '/system-data/overview',
  99  |       '/system-config/user',
  100 |       '/account',
  101 |     ];
  102 | 
  103 |     for (const path of paths) {
  104 |       await navigateTo(page, path);
  105 |       // 页面不应白屏
  106 |       const bodyText = await page.textContent('body');
  107 |       expect(bodyText).toBeTruthy();
  108 |       expect(bodyText!.length).toBeGreaterThan(0);
  109 | 
  110 |       await takeScreenshot(
  111 |         page,
  112 |         `02-path-${path.replace(/\//g, '_').replace(/^_/, '') || 'home'}`
  113 |       );
  114 |     }
  115 | 
  116 |     expect(filterApiErrors(errors)).toEqual([]);
  117 |   });
  118 | });
  119 | 
```