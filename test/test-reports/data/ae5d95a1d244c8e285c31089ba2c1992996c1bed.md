# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 03-homepage.spec.ts >> 03 首页/数据看板测试 >> 03-01 首页应正确渲染无崩溃
- Location: test/e2e/03-homepage.spec.ts:26:7

# Error details

```
Error: expect(received).toBeGreaterThan(expected)

Expected: > 10
Received:   3
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import {
  3  |   bypassLogin,
  4  |   navigateTo,
  5  |   takeScreenshot,
  6  |   collectErrors,
  7  | } from '../utils/test-helpers';
  8  | 
  9  | const filterApiErrors = (errors: string[]) =>
  10 |   errors.filter(
  11 |     (e) =>
  12 |       !e.includes('Failed to fetch') &&
  13 |       !e.includes('NetworkError') &&
  14 |       !e.includes('net::ERR') &&
  15 |       !e.includes('404') &&
  16 |       !e.includes('502') &&
  17 |       !e.includes('503') &&
  18 |       !e.includes('ERR_CONNECTION_REFUSED')
  19 |   );
  20 | 
  21 | test.describe('03 首页/数据看板测试', () => {
  22 |   test.beforeEach(async ({ page }) => {
  23 |     await bypassLogin(page);
  24 |   });
  25 | 
  26 |   test('03-01 首页应正确渲染无崩溃', async ({ page }) => {
  27 |     const errors = collectErrors(page);
  28 |     await navigateTo(page, '/');
  29 | 
  30 |     // 页面应有内容
  31 |     const bodyText = await page.textContent('body');
  32 |     expect(bodyText).toBeTruthy();
> 33 |     expect(bodyText!.length).toBeGreaterThan(10);
     |                              ^ Error: expect(received).toBeGreaterThan(expected)
  34 | 
  35 |     // 不应出现白屏错误
  36 |     const errorPage = page.locator(
  37 |       'text=Something went wrong, text=Application error, text=页面错误'
  38 |     );
  39 |     const hasError = await errorPage.count().catch(() => 0);
  40 |     expect(hasError).toBe(0);
  41 | 
  42 |     await takeScreenshot(page, '03-homepage-rendered');
  43 |     expect(filterApiErrors(errors)).toEqual([]);
  44 |   });
  45 | 
  46 |   test('03-02 首页应包含侧边栏和内容区域', async ({ page }) => {
  47 |     const errors = collectErrors(page);
  48 |     await navigateTo(page, '/');
  49 | 
  50 |     // 侧边栏
  51 |     const sidebar = page
  52 |       .locator('.ant-layout-sider, aside, [class*="sider"]')
  53 |       .first();
  54 |     await expect(sidebar).toBeVisible({ timeout: 10000 });
  55 | 
  56 |     // 内容区域
  57 |     const content = page
  58 |       .locator('.ant-layout-content, main, [class*="content"]')
  59 |       .first();
  60 |     await expect(content).toBeVisible({ timeout: 10000 });
  61 | 
  62 |     await takeScreenshot(page, '03-homepage-layout');
  63 |     expect(filterApiErrors(errors)).toEqual([]);
  64 |   });
  65 | });
  66 | 
```