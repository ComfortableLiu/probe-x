export enum FormItemType {
  TEXT = 'text',
  SELECT = 'select',
  CASCADER = 'cascader',
  DATE = 'date',
  DATE_TIME = 'date_time',
  TIME = 'time',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  SWITCH = 'switch',
  UPLOAD = 'upload',
  NUMBER = 'number',
  RATE = 'rate',
  SLIDER = 'slider',
  // 自定义组件
  CUSTOM = 'custom',
}
